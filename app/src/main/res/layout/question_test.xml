<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/TxtQuestionTitle"
        android:layout_width="match_parent"
        android:layout_height="104dp"
        android:text="Question 1"
        android:textSize="33sp" />

    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="59dp"
        android:layout_marginTop="26dp"
        android:layout_marginEnd="59dp">

        <RadioButton
            android:id="@+id/radioButton"
            android:layout_width="294dp"
            android:layout_height="83dp"
            android:layout_below="@+id/textView"
            android:layout_alignStart="@+id/TxtQuestionDescription"
            android:layout_alignParentEnd="true"
            android:layout_marginStart="-1dp"
            android:layout_marginTop="33dp"
            android:layout_marginEnd="241dp"
            android:text="RadioButto111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111n" />
    </RadioGroup>

    <TextView
        android:id="@+id/textView"
        android:layout_width="293dp"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="59dp"
        android:layout_marginTop="47dp"
        android:layout_marginEnd="59dp"
        android:text="TextView；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；；" />

</RelativeLayout>
